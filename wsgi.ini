[uwsgi]

chdir = /app
pythonpath = ..
module = app:appbase
callable = app
uid = nginx
gid = nginx
socket = /run/one.sock
chmod-socket    = 664
pidfile = /run/.pid
processes = 4
threads = 2
enable-threads = true
vacuum = true

