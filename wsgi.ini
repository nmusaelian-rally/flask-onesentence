[uwsgi]

chdir = /app
pythonpath = ..
module = app:appbase
callable = app
uid = nginx
gid = nginx
socket = /run/one.sock
chmod-socket    = 664
pidfile = /run/uwsgi.pid
processes = 4
threads = 2
enable-threads = true
vacuum = true
die-on-term = true

